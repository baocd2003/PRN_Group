@page
@model ICQS_Management.Pages.BatchDetailsManagement.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BatchDetail[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BatchDetail[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BatchDetail[0].Materials)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BatchDetail[0].Batch)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BatchDetail) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Materials.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Batch.BatchId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BatchDetailId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BatchDetailId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BatchDetailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
